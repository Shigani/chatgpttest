{% if block.settings.text != blank %}
  <div class="hero-slider__heading" role="heading" aria-level="2" {{ block.shopify_attributes }}>{{ block.settings.text }}</div>
{% endif %}

{% schema %}
{
  "name": "t:blocks.hero_text.name",
  "settings": [
    {
      "type": "richtext",
      "id": "text",
      "label": "t:labels.text",
      "default": "<p>Softness in comfort</p>"
    }
  ],
  "presets": [
    {
      "name": "t:blocks.hero_text.name"
    }
  ]
}
{% endschema %}
